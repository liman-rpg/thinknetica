- if subscription.subscriptions.find_by(user: current_user)
  = link_to 'unsubscribe', subscription_path(subscription), method: :delete, remote: true
- else
  = link_to 'subscribe', subscriptions_path(subscription), method: :post, remote: true
