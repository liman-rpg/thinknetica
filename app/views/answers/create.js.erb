<% if @answer.errors.present? %>
  <% @answer.errors.full_messages.each do |massage|%>
    $('#answer_body').before('<%= j massage%>')
  <% end %>
<% else %>
  $('.answers').append('<%= j render @answer %>');
  $('#answer_body').val('');
<% end %>
